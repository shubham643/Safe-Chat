var HashMap = require('hashmap');
var hmap = new HashMap();

//store the different specifications of the app

// This time is in term of number of days.
hmap.set('TOKEN_EXPIRE_TIME',0.1);

module.exports = hmap;